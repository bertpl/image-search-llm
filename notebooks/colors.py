"""
Some shared colors for use in the notebook, so we have a consistent color scheme.
"""

CLR_BLUE = "#6688ff"  # blue color for histogram  (consistent with brtp.lu)
CLR_GREY = (0.5, 0.5, 0.5)  # grey color for histogram
CLR_GREEN = (0.5, 0.9, 0.6)
CLR_DARK_GREEN = tuple([0.8 * v for v in CLR_GREEN])
CLR_BLACK = (0, 0, 0)
